starterCode: |
  var builder = DistributedApplication.CreateBuilder(args);

  // TODO: Create a custom resource class called FeatureFlagResource
  // that inherits from Resource and implements IResourceWithConnectionString
  //
  // Then register it with the builder using:
  //   var flags = builder.AddResource(new FeatureFlagResource("flags"))
  //       .WithEndpoint("http", e => { e.Port = 9090; e.UriScheme = "http"; });
  //
  // Add a custom annotation called FeatureFlagCountAnnotation with a Count property
  //
  // Finally, add a project that references the feature flag service

  builder.Build().Run();
solutionCode: |
  var builder = DistributedApplication.CreateBuilder(args);

  var flags = builder.AddResource(new FeatureFlagResource("flags"))
      .WithEndpoint("http", e => { e.Port = 9090; e.UriScheme = "http"; })
      .WithAnnotation(new FeatureFlagCountAnnotation { Count = 42 });

  builder.AddProject<Projects.Api>("api")
      .WithReference(flags);

  builder.Build().Run();

  class FeatureFlagResource(string name) : Resource(name), IResourceWithConnectionString
  {
      public ReferenceExpression ConnectionStringExpression =>
          ReferenceExpression.Create($"http://flags:9090");
  }

  class FeatureFlagCountAnnotation : IResourceAnnotation
  {
      public int Count { get; set; }
  }
testCases:
  - type: code-contains
    description: "Must define a FeatureFlagResource class"
    value: "FeatureFlagResource"
  - type: code-contains
    description: "Must implement IResourceWithConnectionString"
    value: "IResourceWithConnectionString"
  - type: code-contains
    description: "Must define a custom annotation"
    value: "IResourceAnnotation"
  - type: code-contains
    description: "Must use WithAnnotation"
    value: "WithAnnotation"
  - type: code-contains
    description: "Must add a project with WithReference"
    value: "WithReference"
hints:
  - "Create a class that inherits from Resource and implements IResourceWithConnectionString"
  - "IResourceWithConnectionString requires a ConnectionStringExpression property"
  - "Use builder.AddResource(new YourResource(\"name\")) to register a custom resource"
  - "Create an annotation class implementing IResourceAnnotation"
requiredPackages: []
