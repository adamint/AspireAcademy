instructions: |
  # Cached API with Message Bus

  Build an API with Redis distributed caching and RabbitMQ event publishing.

  **What to do:**
  1. Register Redis distributed cache for connection `"cache"`
  2. Register RabbitMQ client for connection `"messaging"`
  3. Implement cache-first product lookup in GET `/products/{id}`
  4. Implement restock with event publishing in POST `/products/{id}/restock`

  **Key concepts:**
  - `AddRedisDistributedCache(connectionName)` registers `IDistributedCache`
  - `AddRabbitMQClient(connectionName)` registers RabbitMQ `IConnection`
  - Cache-aside pattern: check cache → fallback to data source → populate cache
  - RabbitMQ.Client 7.x uses async API: `CreateChannelAsync()` and `BasicPublishAsync()`
starterCode: |
  using Microsoft.Extensions.Caching.Distributed;
  using RabbitMQ.Client;

  var builder = WebApplication.CreateBuilder(args);

  // TODO: Register Redis distributed cache for "cache"
  // TODO: Register RabbitMQ client for "messaging"

  var app = builder.Build();

  app.MapGet("/products/{id}", async (string id, IDistributedCache cache) =>
  {
      // TODO: Try cache first, then fallback
  });

  app.MapPost("/products/{id}/restock", async (string id, int qty, IConnection rmq) =>
  {
      // TODO: Update and publish event to "inventory.restocked"
  });

  app.Run();
solutionCode: |
  using Microsoft.Extensions.Caching.Distributed;
  using RabbitMQ.Client;

  var builder = WebApplication.CreateBuilder(args);

  builder.AddRedisDistributedCache("cache");
  builder.AddRabbitMQClient("messaging");

  var app = builder.Build();

  app.MapGet("/products/{id}", async (string id, IDistributedCache cache) =>
  {
      var cached = await cache.GetStringAsync($"product:{id}");
      if (cached is not null)
      {
          return Results.Ok(cached);
      }
      return Results.NotFound();
  });

  app.MapPost("/products/{id}/restock", async (string id, int qty, IConnection rmq) =>
  {
      using var channel = await rmq.CreateChannelAsync();
      var body = System.Text.Encoding.UTF8.GetBytes($"{{\"id\":\"{id}\",\"qty\":{qty}}}");
      await channel.BasicPublishAsync("inventory.restocked", "", body);
      return Results.Ok("Restocked");
  });

  app.Run();
testCases:
  - id: tc1
    name: "Compiles successfully"
    type: compiles
    expected: null
    description: "Code must compile without errors"
  - id: tc2
    name: "Registers Redis distributed cache"
    type: code-contains
    expected: "AddRedisDistributedCache"
    description: "Code must register Redis distributed cache"
  - id: tc3
    name: "Registers RabbitMQ client"
    type: code-contains
    expected: "AddRabbitMQClient"
    description: "Code must register RabbitMQ client"
  - id: tc4
    name: "Reads from cache"
    type: code-contains
    expected: "GetStringAsync"
    description: "Code must read from Redis cache"
  - id: tc5
    name: "Publishes to RabbitMQ"
    type: code-contains
    expected: "BasicPublishAsync"
    description: "Code must publish events to RabbitMQ using BasicPublishAsync"
hints:
  - "Use AddRedisDistributedCache(\"cache\")"
  - "Use AddRabbitMQClient(\"messaging\")"
  - "Create a channel with `await rmq.CreateChannelAsync()` (RabbitMQ.Client 7.x uses async API)"
  - "Publish with `await channel.BasicPublishAsync(exchange, routingKey, body)`"
requiredPackages:
  - "Aspire.StackExchange.Redis.DistributedCaching"
  - "Aspire.RabbitMQ.Client"
sortOrder: 0
