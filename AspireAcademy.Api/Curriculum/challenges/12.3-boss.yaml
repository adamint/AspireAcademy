instructions: |
  Build a complete custom hosting integration for a hypothetical "MetricsDB" service.

  **Your tasks:**
  1. Define `MetricsDbResource` inheriting `ContainerResource` and implementing `IResourceWithConnectionString`
  2. Implement `ConnectionStringExpression` with endpoint properties
  3. Write an `AddMetricsDb()` extension method (image: `"metricsdb/server"`, port 9090)
  4. Add an HTTP health check
  5. Subscribe to `ResourceReadyEvent` filtered to the MetricsDB resource to log when it's ready
  6. Register it in an AppHost and wire a project to reference it with `WaitFor`
starterCode: '// Build a complete hosting integration for a hypothetical "MetricsDB" service:

  // 1. MetricsDbResource inheriting ContainerResource + IResourceWithConnectionString

  // 2. AddMetricsDb extension method (image: metricsdb/server, port: 9090)

  // 3. Health check annotation

  // 4. Subscribe to ResourceReadyEvent to log when MetricsDB is ready

  // 5. Register it in an AppHost with another service referencing it


  // Your code here:'
solutionCode: "public class MetricsDbResource(string name) :\n    ContainerResource(name), IResourceWithConnectionString\n{\n    internal const string EndpointName = \"http\";\n    private EndpointReference?\
  \ _endpoint;\n    public EndpointReference Endpoint => _endpoint ??= new(this, EndpointName);\n\n    public ReferenceExpression ConnectionStringExpression =>\n        ReferenceExpression.Create(\n   \
  \         $\"http://{Endpoint.Property(EndpointProperty.Host)}:{Endpoint.Property(EndpointProperty.Port)}\");\n}\n\npublic static class MetricsDbExtensions\n{\n    public static IResourceBuilder<MetricsDbResource>\
  \ AddMetricsDb(\n        this IDistributedApplicationBuilder builder, string name)\n    {\n        return builder.AddResource(new MetricsDbResource(name))\n            .WithImage(\"metricsdb/server\"\
  )\n            .WithImageTag(\"latest\")\n            .WithHttpEndpoint(targetPort: 9090, name: MetricsDbResource.EndpointName)\n            .WithHealthCheck(MetricsDbResource.EndpointName);\n    }\n\
  }\n\n// Usage:\nvar builder = DistributedApplication.CreateBuilder(args);\n\nvar metrics = builder.AddMetricsDb(\"metrics\");\n\nbuilder.Eventing.Subscribe<ResourceReadyEvent>(\n    metrics.Resource,\n\
  \    async (evt, ct) => Console.WriteLine(\"MetricsDB ready!\"));\n\nbuilder.AddProject<Projects.Api>(\"api\")\n    .WithReference(metrics)\n    .WaitFor(metrics);\n\nbuilder.Build().Run();"
testCases:
- type: code-pattern
  id: "t1"
  name: "Must define MetricsDbResource class"
  description: "Must define a class named MetricsDbResource"
  value: "class\\s+MetricsDbResource"
- type: code-contains
  id: "t2"
  name: "Must inherit ContainerResource"
  description: "MetricsDbResource must inherit from ContainerResource"
  value: "ContainerResource(name)"
- type: code-contains
  id: "t3"
  name: "Must implement IResourceWithConnectionString"
  description: "Must implement the IResourceWithConnectionString interface"
  value: "IResourceWithConnectionString"
- type: code-contains
  id: "t4"
  name: "Must implement ConnectionStringExpression"
  description: "Must define ConnectionStringExpression property"
  value: "ConnectionStringExpression"
- type: code-contains
  id: "t5"
  name: "Must add HTTP health check"
  description: "Must call .WithHealthCheck() for health monitoring"
  value: ".WithHealthCheck("
- type: code-contains
  id: "t6"
  name: "Must subscribe to ResourceReadyEvent"
  description: "Must call Subscribe<ResourceReadyEvent> for lifecycle events"
  value: "Subscribe<ResourceReadyEvent>"
- type: code-contains
  id: "t7"
  name: "Must use .WithReference() to wire services"
  description: "Must chain .WithReference() to wire dependencies"
  value: ".WithReference("
hints:
- 'Follow the same pattern as MeilisearchResource: inherit ContainerResource, implement IResourceWithConnectionString'
- ConnectionStringExpression uses ReferenceExpression.Create with endpoint properties
- The extension method should call AddResource, WithImage, WithHttpEndpoint, WithHealthCheck
- Subscribe to ResourceReadyEvent with the resource as filter
- Wire it to a project with WithReference and WaitFor
requiredPackages: []
