# Templates and the Template Catalog

You're starting a new project and need the boilerplate — AppHost, ServiceDefaults, a starter API, maybe a frontend. Or your team wants a standardized template that includes your company's shared health checks and pre-configured Azure integrations so every new service starts from the same foundation. `aspire new` scaffolds projects from templates, and you can create your own.

---

## Built-In Templates

| Template | Description | Languages |
|---|---|---|
| `aspire-starter` | Full app: AppHost + ServiceDefaults + API + frontend | C# |
| `aspire-empty` | Minimal AppHost with just CreateBuilder | C# |
| `aspire-apphost-singlefile` | Single-file AppHost with #: directives | C# |
| `aspire-ts-cs-starter` | TypeScript AppHost + C# API | TypeScript/C# |
| `aspire-js-frontend-starter` | JavaScript frontend with Aspire | JavaScript |
| `aspire-xunit` | xUnit test project with Aspire testing | C# |

## Using Templates

```bash
aspire new                                  # interactive
aspire new aspire-starter --name MyApp      # direct
aspire new aspire-ts-cs-starter --name Bazaar # TypeScript
```

## What Gets Generated

For `aspire-starter --name Bazaar`:

```
Bazaar/
    Bazaar.sln
    Bazaar.AppHost/
        Program.cs         - AppHost with API + frontend
    Bazaar.ServiceDefaults/
        Extensions.cs      - OpenTelemetry, health checks, resilience
    Bazaar.Api/
        Program.cs         - Minimal API
    Bazaar.Web/
        Program.cs         - Blazor frontend
```

For `aspire-ts-cs-starter --name Bazaar`:

```
Bazaar/
    apphost.ts             - TypeScript AppHost
    .modules/aspire.js     - Generated TypeScript SDK
    package.json
    aspire.config.json
```

## Template Internals

Templates live in `src/Aspire.ProjectTemplates/templates/`. Each has a `.template.config/template.json` defining shortName, parameters, and postActions (dotnet restore, npm install, code generation).

> 🐛 **Common Gotcha:** When using `aspire new` with the TypeScript template (`aspire-ts-cs-starter`), the generated `apphost.ts` imports from `.modules/aspire.js` — a file that's auto-generated during `npm install`. If you skip the post-create npm install or delete `node_modules`, the AppHost won't compile.

```terminal
{"command": "aspire new aspire-starter --name Bazaar", "output": ["Creating project from template 'aspire-starter'...", "", "  Bazaar/", "    Bazaar.sln", "    Bazaar.AppHost/Program.cs", "    Bazaar.ServiceDefaults/Extensions.cs", "    Bazaar.Api/Program.cs", "    Bazaar.Web/Program.cs", "", "Running post-create actions:", "  dotnet restore Bazaar.sln", "  Restore completed.", "", "Project 'Bazaar' created successfully.", "Run 'cd Bazaar && aspire run' to start."], "speed": 30}
```

## Custom Templates

Create your own following the `dotnet new` template format:

```bash
dotnet new install ./path/to/my-template/
aspire new my-custom-template --name MyProject
```

```reveal
{"question": "What is the relationship between aspire new and dotnet new for templates?", "answer": "aspire new is built on top of the dotnet new template engine. Aspire templates use the same .template.config/template.json format and are installed via dotnet new install. The aspire new command adds Aspire-specific post-creation actions (like generating .modules/aspire.js for TypeScript AppHosts) and provides a curated interactive experience, but under the hood it delegates to the same template engine."}
```

```scenario
{"situation": "Your team wants a standardized Aspire project template that includes your company's custom health check endpoints, shared ServiceDefaults, and pre-configured Azure integrations. What is the best approach?", "options": [{"label": "Fork the aspire-starter template and modify it", "outcome": "This works initially but creates a maintenance burden — you'll need to manually merge upstream changes from Aspire's evolving templates.", "correct": false}, {"label": "Create a custom dotnet new template with your additions, then install it so aspire new can use it", "outcome": "Correct! Create a new template with .template.config/template.json that includes your company's ServiceDefaults, health checks, and Azure integrations. Install it with dotnet new install, and it becomes available through aspire new for the whole team.", "correct": true}, {"label": "Write a shell script that runs aspire new and then applies modifications", "outcome": "Fragile approach. Scripts break when template output changes. A proper template is self-contained and version-controlled.", "correct": false}]}
```
