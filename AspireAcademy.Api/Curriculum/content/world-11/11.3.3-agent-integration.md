# Agent Integration

Aspire auto-detects AI agent environments and configures the MCP server for each one.

---

## Supported Agents

| Agent | Scanner |
|---|---|
| **Claude Code** | `ClaudeCodeAgentEnvironmentScanner` |
| **VS Code Copilot** | `VsCodeAgentEnvironmentScanner` |
| **OpenCode** | `OpenCodeAgentEnvironmentScanner` |
| **Playwright** | `PlaywrightSupport` |
| **GitHub Copilot CLI** | `CopilotCliRunner` |

## How Auto-Detection Works

When you run `aspire agent mcp`, the CLI:

1. Scans environment variables and parent process information
2. Each `AgentEnvironmentScanner` checks for its agent's indicators
3. The matching scanner configures the MCP session (tool filtering, output formatting)
4. Default configuration is used if no agent is detected

## Using Aspire with Claude Code

```
User: "What resources are running?"
Claude: [calls list_resources]
        "6 resources: pg (Running), cache (Running), api (Running),
         frontend (Running), ml (FailedToStart), worker (Running)"

User: "Why did ml fail?"
Claude: [calls list_console_logs("ml")]
        "ModuleNotFoundError: No module named 'scikit-learn'"

User: "Show me the slowest trace"
Claude: [calls list_traces]
        "Trace abc123: GET /search took 3.2s
         Bottleneck: ML service query at 2.8s"
```

## Using Aspire with VS Code Copilot

The extension auto-registers the MCP server. In Copilot Chat:

```
@workspace What Aspire resources are running?
@workspace Show me the logs from the API
@workspace Search the Aspire docs for health checks
```

Copilot uses list_resources, list_console_logs, and search_docs MCP tools.

## Agent Scanner Architecture

```
src/Aspire.Cli/Agents/
    CommonAgentApplicators.cs    - shared logic
    ClaudeCode/                  - Claude detection
    VsCode/                      - VS Code detection
    OpenCode/                    - OpenCode detection
    Playwright/                  - Playwright detection
    CopilotCli/                  - GitHub Copilot CLI
```

CommonAgentApplicators handles tool registration, backchannel setup, and notification routing. Agent-specific scanners handle detection and formatting.

```deepdive
{"title": "How Agent Auto-Detection Works Under the Hood", "content": "Each AgentEnvironmentScanner implements a detection method that checks environment variables, parent process names, and file system markers. For example, ClaudeCodeAgentEnvironmentScanner looks for the CLAUDE_CODE environment variable. VsCodeAgentEnvironmentScanner checks for VSCODE_PID and TERM_PROGRAM=vscode. The scanners run in priority order, and the first match wins. When matched, the scanner configures output formatting and tool descriptions optimized for that agent — Claude Code handles long JSON well, while VS Code Copilot Chat works better with shorter responses. If no scanner matches, default configuration is used with all tools enabled and standard JSON formatting."}
```

```scenario
{"situation": "You're building a custom AI agent that needs to work with Aspire's MCP server. How does your agent get detected and configured?", "options": [{"label": "You must write a custom AgentEnvironmentScanner and contribute it to the Aspire repository", "outcome": "Not required. While you could contribute a scanner, the MCP server works with any agent using the default configuration.", "correct": false}, {"label": "The MCP server uses default configuration if no scanner matches — your agent works out of the box with all tools available", "outcome": "Correct! The auto-detection is an optimization, not a requirement. Unrecognized agents get the default configuration with all MCP tools enabled. You can start building immediately without any Aspire-side changes.", "correct": true}, {"label": "You must set a specific environment variable that Aspire recognizes", "outcome": "Incorrect. While known agents are detected via environment variables, unknown agents still work with the default configuration.", "correct": false}]}
```

```reveal
{"question": "Why does Aspire auto-detect the AI agent environment instead of using a generic configuration?", "answer": "Different agents have different capabilities and preferences. Claude Code handles long JSON responses well, while VS Code Copilot Chat works better with shorter, formatted outputs. The scanner system lets Aspire optimize tool descriptions, output formatting, and notification behavior for each agent — improving the AI's ability to use Aspire tools effectively without manual configuration."}
```
