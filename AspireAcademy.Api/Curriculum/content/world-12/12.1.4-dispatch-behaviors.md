# Dispatch Behaviors

When publishing an event, you choose how handlers execute. The four behaviors trade off between blocking/non-blocking and sequential/concurrent execution.

---

## The Enum

```csharp
// src/Aspire.Hosting/Eventing/EventDispatchBehavior.cs

public enum EventDispatchBehavior
{
    /// <summary>
    /// Fires events sequentially and blocks until they are all processed.
    /// </summary>
    BlockingSequential,

    /// <summary>
    /// Fires events concurrently and blocks until they are all processed.
    /// </summary>
    BlockingConcurrent,

    /// <summary>
    /// Fires events sequentially but does not block.
    /// </summary>
    NonBlockingSequential,

    /// <summary>
    /// Fires events concurrently but does not block.
    /// </summary>
    NonBlockingConcurrent
}
```

## How Each Behavior Executes (from the source)

The actual dispatch logic in `DistributedApplicationEventing.PublishAsync`:

```csharp
public async Task PublishAsync<T>(T @event,
    EventDispatchBehavior dispatchBehavior, CancellationToken ct)
{
    if (_eventSubscriptionListLookup.TryGetValue(typeof(T), out var subscriptions))
    {
        if (dispatchBehavior is BlockingConcurrent or NonBlockingConcurrent)
        {
            // Start all handlers simultaneously
            var pending = new List<Task>(subscriptions.Count);
            foreach (var sub in subscriptions.ToArray())
            {
                pending.Add(sub.Callback(@event, ct));
            }

            if (dispatchBehavior == NonBlockingConcurrent)
            {
                // Fire and forget via Task.Run
                _ = Task.Run(async () =>
                    await Task.WhenAll(pending).ConfigureAwait(false));
            }
            else
            {
                // Block until all complete
                await Task.WhenAll(pending).ConfigureAwait(false);
            }
        }
        else // Sequential
        {
            if (dispatchBehavior == NonBlockingSequential)
            {
                _ = Task.Run(async () =>
                {
                    foreach (var sub in subscriptions.ToArray())
                        await sub.Callback(@event, ct).ConfigureAwait(false);
                });
            }
            else // BlockingSequential (default)
            {
                foreach (var sub in subscriptions.ToArray())
                    await sub.Callback(@event, ct).ConfigureAwait(false);
            }
        }
    }
}
```

Key observations:
- **`subscriptions.ToArray()`** — creates a snapshot to avoid issues if a handler adds/removes subscriptions
- **Non-blocking variants use `Task.Run`** — the fire-and-forget pattern
- **Default is `BlockingSequential`** — calling `PublishAsync(@event)` without a behavior uses this

## When to Use Each

| Behavior | Use Case |
|---|---|
| **BlockingSequential** | Validation, state modification, must complete before lifecycle continues |
| **BlockingConcurrent** | Independent setup tasks that must finish but can run in parallel |
| **NonBlockingSequential** | Notifications, logging — ordered but lifecycle should not wait |
| **NonBlockingConcurrent** | Independent background tasks — fan-out notifications |

Most framework events use `BlockingSequential` because handlers may modify the app model before the next lifecycle phase.

> 🐛 **Common Gotcha:** With `NonBlockingConcurrent` and `NonBlockingSequential`, exceptions in handlers are silently swallowed inside `Task.Run`. If your handler can fail, use a Blocking variant so exceptions propagate — or add explicit try/catch logging in your non-blocking handlers.

```compare
{"before": {"label": "BlockingSequential (Default)", "code": "// Handlers run one at a time, publisher waits\nawait eventing.PublishAsync(evt);\n// All handlers finished here\nConsole.WriteLine(\"Safe to proceed\");"}, "after": {"label": "NonBlockingConcurrent", "code": "// Handlers run in parallel via Task.Run\nawait eventing.PublishAsync(evt,\n  EventDispatchBehavior.NonBlockingConcurrent);\n// Handlers may still be running!\nConsole.WriteLine(\"Might not be done\");"}, "caption": "BlockingSequential guarantees all handlers complete before the publisher continues. NonBlockingConcurrent fires and forgets — the publisher returns immediately while handlers run in parallel on background threads."}
```

```scenario
{"situation": "You're writing a BeforeStartEvent handler that validates every resource has required annotations. If validation fails, you want to halt the AppHost. Which dispatch behavior should this event use?", "options": [{"label": "NonBlockingConcurrent — validate quickly in parallel without blocking startup", "outcome": "Dangerous! Non-blocking means the publisher won't wait for your handler. The AppHost will continue starting resources before your validation completes, and if you throw, the exception is swallowed.", "correct": false}, {"label": "BlockingSequential — validate in order, halt if anything fails", "outcome": "Correct! BlockingSequential is the default for framework events precisely because handlers like validation MUST complete (and can throw) before the lifecycle continues. This is why BeforeStartEvent uses it.", "correct": true}, {"label": "BlockingConcurrent — validate in parallel but wait for completion", "outcome": "This would work but introduces a subtle issue: if two validation handlers both modify shared state (like a validation error list), they'd race. Sequential is safer for handlers that may interact.", "correct": false}]}
```

```reveal
{"question": "Why does PublishAsync call subscriptions.ToArray() before iterating?", "answer": "It creates a snapshot of the subscription list at the moment of publishing. Without this, a handler could subscribe or unsubscribe during iteration (e.g., a handler that unsubscribes itself after first invocation), which would throw a ConcurrentModificationException. The ToArray() ensures the iteration is safe even if the subscription list changes during dispatch."}
```
