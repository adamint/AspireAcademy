# Resource-Level Events

Resource events fire for individual resources. Each carries the `IResource` that triggered it and the `IServiceProvider` for accessing services.

---

## The Complete Catalog

### BeforeResourceStartedEvent

Fires just before DCP starts a specific resource:

```csharp
public class BeforeResourceStartedEvent(
    IResource resource,
    IServiceProvider services) : IDistributedApplicationResourceEvent
{
    public IResource Resource { get; } = resource;
    public IServiceProvider Services { get; } = services;
}
```

### ResourceReadyEvent

Fires the **first time** a resource transitions to a healthy running state. This is the event that `WaitFor` uses internally:

```csharp
public class ResourceReadyEvent(
    IResource resource,
    IServiceProvider services) : IDistributedApplicationResourceEvent
{
    public IResource Resource => resource;
    public IServiceProvider Services => services;
}
```

### ConnectionStringAvailableEvent

Fires when a resource's connection string becomes available (resolved from placeholders to actual values):

```csharp
public class ConnectionStringAvailableEvent(
    IResource resource,
    IServiceProvider services) : IDistributedApplicationResourceEvent
{
    public IResource Resource => resource;
    public IServiceProvider Services => services;
}
```

### ResourceStoppedEvent

Fires when a resource exits or is stopped. Includes the final `ResourceEvent` snapshot:

```csharp
public class ResourceStoppedEvent(
    IResource resource,
    IServiceProvider services,
    ResourceEvent resourceEvent) : IDistributedApplicationResourceEvent
{
    public IResource Resource { get; } = resource;
    public IServiceProvider Services { get; } = services;
    public ResourceEvent ResourceEvent { get; } = resourceEvent;
}
```

### ResourceEndpointsAllocatedEvent

Fires when DCP allocates endpoints (port numbers) for a specific resource:

```csharp
public class ResourceEndpointsAllocatedEvent(
    IResource resource,
    IServiceProvider services) : IDistributedApplicationResourceEvent
{
    public IResource Resource { get; } = resource;
    public IServiceProvider Services { get; } = services;
}
```

### InitializeResourceEvent

The richest event — fires as a signal to initialize a resource. Provides direct access to eventing, logging, and notification services:

```csharp
public class InitializeResourceEvent(
    IResource resource,
    IDistributedApplicationEventing distributedApplicationEventing,
    ResourceLoggerService resourceLoggerService,
    ResourceNotificationService resourceNotificationService,
    IServiceProvider services) : IDistributedApplicationResourceEvent
{
    public IResource Resource { get; } = resource;
    public IDistributedApplicationEventing Eventing { get; }
        = distributedApplicationEventing;
    public ILogger Logger { get; } = resourceLoggerService.GetLogger(resource);
    public ResourceNotificationService Notifications { get; }
        = resourceNotificationService;
    public IServiceProvider Services { get; } = services;
}
```

`InitializeResourceEvent` is unique — it provides `Eventing`, `Logger`, and `Notifications` directly, so your handler can subscribe to further events, log to the resource's stream, and publish state updates without resolving them from DI.

> 🌍 **Real World:** `ResourceReadyEvent` is the engine behind `WaitFor`. When you write `api.WaitFor(db)`, Aspire subscribes to `ResourceReadyEvent` for the database. Until that event fires, the API stays in `Waiting` state. This is why health checks matter — without them, `ResourceReadyEvent` fires as soon as the container is running, even if the database isn't accepting connections yet.

```reveal
{"question": "What makes InitializeResourceEvent different from all other resource events?", "answer": "InitializeResourceEvent is the only resource event that provides direct access to the Eventing system, a pre-configured ILogger for the resource's log stream, AND the ResourceNotificationService. Other resource events only carry IResource and IServiceProvider — you'd have to resolve those services from DI yourself. This makes InitializeResourceEvent the ideal hook for setting up complex per-resource initialization that needs to subscribe to further events or publish state updates."}
```

```scenario
{"situation": "Your custom resource needs to wait until its connection string is available, then run a database migration before the resource is considered 'ready'. Which events do you subscribe to?", "options": [{"label": "Subscribe to BeforeResourceStartedEvent and run the migration there", "outcome": "The resource hasn't started yet at this point, so the connection string isn't available. The migration would fail trying to connect.", "correct": false}, {"label": "Subscribe to ConnectionStringAvailableEvent to run the migration, then let ResourceReadyEvent fire naturally", "outcome": "Correct! ConnectionStringAvailableEvent fires when the connection string resolves to actual values. You can run the migration at that point. ResourceReadyEvent will fire later when health checks pass.", "correct": true}, {"label": "Subscribe to ResourceReadyEvent and run the migration", "outcome": "ResourceReadyEvent fires when the resource is already healthy. If your migration needs to run BEFORE the resource is considered ready, this is too late — other resources waiting on it may have already started.", "correct": false}]}
```

## Subscribing to Resource Events

```csharp
// All resources
builder.Eventing.Subscribe<ResourceReadyEvent>(async (evt, ct) =>
{
    Console.WriteLine($"{evt.Resource.Name} is ready");
});

// Specific resource
var db = builder.AddPostgres("pg").AddDatabase("bazaar-db");
builder.Eventing.Subscribe<ConnectionStringAvailableEvent>(
    db.Resource,
    async (evt, ct) =>
    {
        var connStr = await ((IResourceWithConnectionString)evt.Resource)
            .GetConnectionStringAsync(ct);
        Console.WriteLine($"Connection string: {connStr}");
    });
```

```terminal
{"command": "dotnet run --project AppHost", "output": ["info: Aspire.Hosting[0] Starting AppHost...", "info: Aspire.Hosting[0] BeforeResourceStartedEvent fired for 'pg'", "info: Aspire.Hosting[0] BeforeResourceStartedEvent fired for 'api'", "info: Aspire.Hosting[0] ConnectionStringAvailableEvent: pg connection resolved", "info: Aspire.Hosting[0] ResourceReadyEvent: pg is ready", "info: Aspire.Hosting[0] ResourceReadyEvent: api is ready"], "speed": 35}
```
