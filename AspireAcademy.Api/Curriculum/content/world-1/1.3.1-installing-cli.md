# Installing the Aspire CLI

The Aspire CLI is a cross-platform tool that manages the full lifecycle of your Aspire application — from scaffolding to running to publishing.

---

## Installation

### macOS / Linux

```bash
# Install via the install script (recommended)
curl -sSL https://aspire.dev/install.sh | bash
```

### Windows

```powershell
irm https://aspire.dev/install.ps1 | iex
```

### Verify Installation

```bash
aspire --version
```

## aspire doctor

Before you create your first app, run `aspire doctor` to verify your development environment:

```bash
aspire doctor
```

`aspire doctor` checks:

| Check | What It Verifies |
|---|---|
| **.NET SDK** | Correct SDK version installed (skipped for non-.NET AppHosts) |
| **Dev Certificates** | HTTPS development certificate is trusted |
| **Container Runtime** | Docker (≥28.0) or Podman (≥5.0) is installed and running |
| **Deprecated Workloads** | No deprecated .NET Aspire workloads detected |

If any check fails, `aspire doctor` tells you exactly what to fix. Run it whenever something seems wrong.

```terminal
{"command": "aspire doctor", "output": ["Aspire Doctor", "──────────────", "✔ .NET SDK 9.0.300 .............. pass", "✔ Dev Certificates .............. pass", "✔ Container Runtime (Docker) .... pass", "✔ No deprecated workloads ....... pass", "", "Everything looks good!"], "speed": 40}
```

> 🐛 **Common Gotcha:** The most common `aspire doctor` failure is "Container Runtime not found." This means Docker Desktop (or Podman) isn't running — not that it isn't installed. Check your system tray and start Docker before retrying.

```reveal
{"question": "Why does aspire doctor check for deprecated .NET Aspire workloads?", "answer": "Before the standalone Aspire CLI existed, Aspire was distributed as a .NET workload (installed via 'dotnet workload install aspire'). The standalone CLI replaced this approach, but having both installed can cause conflicts — the old workload might intercept commands meant for the new CLI. aspire doctor detects this and tells you to uninstall the deprecated workload with 'dotnet workload uninstall aspire'."}
```

## What About dotnet run?

You might wonder: do you use `aspire run` or `dotnet run`? Both work for C# AppHosts, but `aspire run` is the recommended entry point because:

- It works for **both** AppHost languages (C# and TypeScript)
- It provides richer console output with resource status
- It supports `--detach` for background execution
- It manages the backchannel communication between CLI and AppHost

For the rest of this course, we use `aspire run`.

```scenario
{"situation": "You clone a colleague's Aspire project and run `aspire run`. It fails with 'Container runtime not found' even though you're certain Docker Desktop is installed. The AppHost uses PostgreSQL and Redis. What's the fastest troubleshooting path?", "options": [{"label": "Reinstall the Aspire CLI — the installation might be corrupted", "outcome": "Unlikely to help. The error specifically says 'Container runtime not found', which points to the container runtime, not the CLI itself."}, {"label": "Run `aspire doctor` and check its output", "outcome": "Correct. `aspire doctor` will identify exactly what's wrong. The most common cause of this error is that Docker Desktop is installed but not currently running — check your system tray. aspire doctor distinguishes between 'not installed' and 'not running' and tells you exactly what to fix.", "correct": true}, {"label": "Pre-pull the PostgreSQL and Redis images manually with `docker pull`", "outcome": "If Docker were running, Aspire would pull images automatically. The error says the runtime itself isn't found, not that images are missing."}, {"label": "Switch to Podman as an alternative container runtime", "outcome": "Aspire does support Podman, but this is overkill. The fix is almost certainly starting Docker Desktop, not switching runtimes. Run `aspire doctor` first to confirm."}]}
```

> 🌍 **Real World:** Teams migrating from Docker Compose typically start by importing their existing `docker-compose.yml` into an AppHost using `AddDockerComposeEnvironment()`, then convert one service at a time to native Aspire resources. This lets you adopt Aspire incrementally without a big-bang migration — the CLI treats imported Compose services and native Aspire resources identically.
