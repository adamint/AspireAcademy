# Your First Integration Test

Walk through writing a complete integration test for a Bazaar API endpoint.

---

## The Test

```csharp
public class SearchEndpointTests : IAsyncLifetime
{
    private DistributedApplication _app = null!;

    public async Task InitializeAsync()
    {
        var builder = await DistributedApplicationTestingBuilder
            .CreateAsync<Projects.BazaarAppHost>();

        _app = await builder.BuildAsync();
        await _app.StartAsync();

        // Wait for resources to be ready after starting
        var rns = _app.Services.GetRequiredService<ResourceNotificationService>();
        await rns.WaitForResourceAsync("api", KnownResourceStates.Running);
    }

    public async Task DisposeAsync() => await _app.DisposeAsync();

    [Fact]
    public async Task SearchReturnsResults()
    {
        // Arrange
        var client = _app.CreateHttpClient("api");

        // Act
        var response = await client.GetAsync("/api/search?q=aspire");

        // Assert
        response.StatusCode.Should().Be(HttpStatusCode.OK);
        var results = await response.Content.ReadFromJsonAsync<SearchResult[]>();
        results.Should().NotBeEmpty();
    }

    [Fact]
    public async Task SearchWithEmptyQueryReturns400()
    {
        var client = _app.CreateHttpClient("api");
        var response = await client.GetAsync("/api/search?q=");
        response.StatusCode.Should().Be(HttpStatusCode.BadRequest);
    }
}
```

## Key Patterns

1. **`IAsyncLifetime`** — xUnit interface for async setup/teardown
2. **`WaitForResourceAsync`** — ensures resources are ready before tests run
3. **`CreateHttpClient("name")`** — gets a client for the named resource
4. **Real responses** — you are testing against real services with real databases

```reveal
{"question": "StartAsync returns a completed Task — so why do we still need WaitForResourceAsync afterward?", "answer": "StartAsync initiates DCP orchestration and returns once the hosting infrastructure is running, but individual resources (containers, projects) may still be initializing. WaitForResourceAsync subscribes to the ResourceNotificationService and blocks until the named resource reports Running via health checks. Without it, your HTTP requests may hit a container mid-initialization — leading to connection refused errors or 503s. This is especially common with databases that need schema migration time."}
```

```scenario
{"situation": "Your integration test for a search endpoint intermittently fails with a ConnectionRefusedException on the first test run of the day, but passes on retries. The test does NOT call WaitForResourceAsync. What is the most likely cause?", "options": [{"label": "The API code has a race condition in the search handler", "outcome": "Unlikely — the error is a connection failure, not a logic error. The API itself isn't receiving the request.", "correct": false}, {"label": "The container image was evicted from Docker cache overnight and the container isn't ready when the test fires", "outcome": "Correct. Without WaitForResourceAsync, the test sends HTTP requests before the container finishes starting. Cold starts (pulling images, initializing databases) make this especially likely on first runs.", "correct": true}, {"label": "xUnit is running tests in the wrong order", "outcome": "xUnit does not guarantee test ordering, but that wouldn't cause a connection failure — it would cause assertion mismatches.", "correct": false}]}
```

> 🐛 **Common Gotcha:** Always wait for *all* resources your test depends on, not just the API. If your API talks to PostgreSQL, wait for both `\"api\"` and `\"pg\"` to reach Running state.

## Running Tests

```bash
dotnet test
```

Tests start DCP, pull container images (first run only), start services, run assertions, and tear everything down.

```timeline
{"steps": [{"title": "DCP Starts", "description": "The Distributed Application Testing Builder initializes DCP, which begins orchestrating container lifecycle.", "icon": "🚀"}, {"title": "Containers Pull & Start", "description": "Docker images are pulled (first run only) and containers start with randomized ports. PostgreSQL, Redis, etc. begin initializing.", "icon": "🐳"}, {"title": "Resource Readiness", "description": "WaitForResourceAsync polls until each resource reports Running via health checks or readiness probes.", "icon": "✅"}, {"title": "Tests Execute", "description": "Your test methods run against live services — real HTTP requests, real database queries, real responses.", "icon": "🧪"}, {"title": "Teardown", "description": "DisposeAsync stops all containers and releases ports. Each test class gets a clean slate.", "icon": "🧹"}]}
```

> 🌍 **Real World:** This test lifecycle mirrors what happens in production Aspire apps — DCP orchestrates containers the same way. Your integration tests exercise the exact startup and teardown path your app uses in deployment.
