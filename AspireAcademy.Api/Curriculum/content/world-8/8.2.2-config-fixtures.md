# Configuration Overlays and Test Fixtures

Share test infrastructure across test classes and override configuration for test scenarios.

---

## Shared Fixtures

xUnit's `ICollectionFixture` lets multiple test classes share one AppHost:

```csharp
[CollectionDefinition("Bazaar")]
public class BazaarCollection : ICollectionFixture<BazaarFixture> { }

public class BazaarFixture : IAsyncLifetime
{
    public DistributedApplication App { get; private set; } = null!;

    public async Task InitializeAsync()
    {
        var builder = await DistributedApplicationTestingBuilder
            .CreateAsync<Projects.BazaarAppHost>();
        App = await builder.BuildAsync();
        await App.StartAsync();
    }

    public async Task DisposeAsync() => await App.DisposeAsync();
}

[Collection("Bazaar")]
public class SearchTests(BazaarFixture fixture)
{
    [Fact]
    public async Task SearchWorks()
    {
        var client = fixture.App.CreateHttpClient("api");
        // ...
    }
}
```

All test classes in the `"Bazaar"` collection share one running AppHost.

```reveal
{"question": "What's the trade-off of sharing one AppHost across multiple test classes via ICollectionFixture?", "answer": "Sharing an AppHost speeds up test execution dramatically — you pay the container startup cost once instead of per-class. But tests in the same collection share state (database rows, cache entries), so they must either clean up after themselves or be designed to tolerate pre-existing data. Tests also run sequentially within a collection, which limits parallelism."}
```

## Configuration Overlays

Override `appsettings.json` values for tests:

```csharp
var builder = await DistributedApplicationTestingBuilder
    .CreateAsync<Projects.BazaarAppHost>();

builder.Configuration["Parameters:api-key"] = "test-api-key-12345";
```

## Parallel Test Execution

By default, xUnit runs test classes in parallel. Since each shared fixture gets its own AppHost:
- Tests within a collection run sequentially (shared state)
- Tests across collections run in parallel (separate AppHosts)

Configure with `xunit.runner.json`:
```json
{
    "parallelizeTestCollections": true,
    "maxParallelThreads": 4
}
```

```compare
{"before": {"label": "Per-Class AppHost (No Fixture)", "code": "public class SearchTests : IAsyncLifetime\n{\n    private DistributedApplication _app = null!;\n\n    public async Task InitializeAsync()\n    {\n        // Each test class starts its own AppHost\n        var builder = await DistributedApplicationTestingBuilder\n            .CreateAsync<Projects.BazaarAppHost>();\n        _app = await builder.BuildAsync();\n        await _app.StartAsync();\n    }\n\n    public async Task DisposeAsync() =>\n        await _app.DisposeAsync();\n}"}, "after": {"label": "Shared Fixture (ICollectionFixture)", "code": "[Collection(\"Bazaar\")]\npublic class SearchTests(BazaarFixture fixture)\n{\n    // All classes in the collection share one AppHost\n    [Fact]\n    public async Task SearchWorks()\n    {\n        var client = fixture.App.CreateHttpClient(\"api\");\n        // ...\n    }\n}\n\n[Collection(\"Bazaar\")]\npublic class CartTests(BazaarFixture fixture)\n{\n    // Same AppHost, no extra startup cost\n}"}, "caption": "Shared fixtures eliminate redundant container startups — one AppHost serves all test classes in the collection"}
```

> 🐛 **Common Gotcha:** If you set `maxParallelThreads` too high, you may exhaust Docker resources. Each parallel collection runs its own AppHost with its own containers. On a CI runner with 4 cores, 2-4 parallel threads is usually optimal.

```scenario
{"situation": "You have 5 test classes: SearchTests, CartTests, and CheckoutTests need the full Bazaar AppHost with PostgreSQL, Redis, and the API. HealthTests and SmokeTests only need the API. How should you organize your test collections?", "options": [{"label": "Put all 5 classes in one collection to share a single AppHost", "outcome": "This works but forces all tests to run sequentially. HealthTests and SmokeTests don't need Redis or PostgreSQL, so they're waiting for unnecessary container startup.", "correct": false}, {"label": "Two collections: one for the full-stack tests (Search, Cart, Checkout) and one for API-only tests (Health, Smoke)", "outcome": "Correct. The full-stack collection shares one AppHost with all dependencies. The API-only collection uses a lighter fixture. Both collections run in parallel, maximizing throughput.", "correct": true}, {"label": "No collections — let each class spin up its own AppHost", "outcome": "This provides maximum isolation but 5x the container startup cost. For a CI pipeline, this could add minutes of overhead.", "correct": false}]}
```
