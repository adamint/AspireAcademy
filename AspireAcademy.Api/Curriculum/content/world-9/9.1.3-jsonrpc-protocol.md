# The JSON-RPC Protocol

Every polyglot AppHost call is a JSON-RPC 2.0 message over a Unix socket. Here is the wire format.

---

## Request Format

```json
{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "invokeCapability",
    "params": {
        "capabilityId": "addRedis",
        "args": {
            "builderHandle": "handle-1",
            "name": "cache"
        }
    }
}
```

## Response Format

```json
{
    "jsonrpc": "2.0",
    "id": 1,
    "result": {
        "$handle": "handle-3"
    }
}
```

The `$handle` key indicates a handle reference — the caller (TypeScript/Python) wraps this in a typed proxy object.

## Error Format

```json
{
    "jsonrpc": "2.0",
    "id": 1,
    "error": {
        "code": -32000,
        "message": "Resource name 'cache' is already in use",
        "data": {
            "type": "CapabilityException",
            "details": "..."
        }
    }
}
```

## AtsMarshaller

The `AtsMarshaller` handles type conversion between JSON and .NET:

| JSON Type | .NET Type | Category |
|---|---|---|
| `{ "$handle": "id" }` | Object reference | Handle type |
| `{ "name": "...", "port": 80 }` | DTO | Value type |
| `"Persistent"` | Enum value | Enum type |
| `"hello"` | `string` | Primitive |
| `42` | `int` | Primitive |
| `true` | `bool` | Primitive |

Handle types are passed by reference (only the ID crosses the wire). DTO types are passed by value (serialized as JSON). This distinction is defined by the Application Type System (ATS).

```reveal
{"question": "Why are handle types passed by reference while DTOs are passed by value? What would break if all types were passed by value?", "answer": "Handle types like IResourceBuilder<RedisResource> hold mutable state, open connections, and method implementations that can't be serialized to JSON. Passing them by value would require serializing the entire .NET object graph — including internal DI references, cancellation tokens, and running tasks. DTOs like CreateBuilderOptions are simple, immutable data containers with no behavior, making value serialization natural and efficient."}
```

```compare
{"before": {"label": "Handle Type (By Reference)", "code": "// TypeScript sends only the handle ID\n{\n  \"method\": \"invokeCapability\",\n  \"params\": {\n    \"capabilityId\": \"withLifetime\",\n    \"args\": {\n      \"resourceHandle\": \"handle-3\",\n      \"lifetime\": \"Persistent\"\n    }\n  }\n}\n// .NET resolves handle-3 → IResourceBuilder<RedisResource>"}, "after": {"label": "DTO Type (By Value)", "code": "// TypeScript sends the full object\n{\n  \"method\": \"invokeCapability\",\n  \"params\": {\n    \"capabilityId\": \"createBuilder\",\n    \"args\": {\n      \"options\": {\n        \"name\": \"bazaar\",\n        \"disableDashboard\": false,\n        \"allowUnsecuredTransport\": true\n      }\n    }\n  }\n}\n// .NET deserializes JSON → CreateBuilderOptions"}, "caption": "Handles cross the wire as opaque IDs; DTOs cross as serialized JSON objects"}
```

> 🎲 **Fun Fact:** The AtsMarshaller automatically detects which serialization strategy to use based on the [AspireExport] and [AspireDto] attributes — developers never manually specify wire format.

## Latency

Since the socket is local, round-trip latency is **microseconds**. A typical AppHost with 10 resources makes ~50 RPC calls during startup — total overhead is well under 10ms.

```scenario
{"situation": "You're debugging a TypeScript AppHost that hangs during startup. You capture the JSON-RPC traffic and see a request with id: 7 was sent but no response was received. The request was invokeCapability('addPostgres', { builderHandle: 'handle-1', name: 'db' }). What is the most likely cause?", "options": [{"label": "Network latency on the Unix socket is causing timeouts", "outcome": "Unix socket latency is microseconds — network delays don't apply to local IPC. This is not a latency issue.", "correct": false}, {"label": "The RemoteHost's CapabilityDispatcher threw an unhandled exception processing the request", "outcome": "Correct. If the .NET side throws an unhandled exception (e.g., the PostgreSQL hosting package isn't loaded), the JSON-RPC response may never be sent. Check RemoteHost logs for unhandled exceptions and verify that Aspire.Hosting.PostgreSQL is in the loaded assemblies.", "correct": true}, {"label": "The 'handle-1' reference expired due to garbage collection", "outcome": "The HandleRegistry holds strong references to all registered objects — they won't be garbage collected while the registry exists.", "correct": false}]}
```

> 🐛 **Common Gotcha:** JSON-RPC error code `-32000` is a generic server error. The `data.type` field contains the actual exception type (e.g., `CapabilityException`, `HandleNotFoundException`). Always check `data.type` for meaningful diagnostics.
